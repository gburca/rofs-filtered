# Process this file with automake to produce Makefile.in

bin_PROGRAMS = rofs_filtered
rofs_filtered_SOURCES = rofs-filtered.c

CFLAGS=-Wall -std=c99 -D_GNU_SOURCE -D_FILE_OFFSET_BITS=64
LDFLAGS=-lfuse
